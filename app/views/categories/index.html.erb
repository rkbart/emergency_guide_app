<!-- app/views/categories/index.html.erb -->
<div class="min-h-screen bg-gray-50 pt-20 pb-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-7xl mx-auto">
    <!-- Page Header -->
    <div class="text-center mb-12">
      <h1 class="text-3xl font-extrabold text-gray-900 sm:text-4xl">
        First Aid Guide
      </h1>
      <p class="mt-3 max-w-2xl mx-auto text-xl text-gray-500 sm:mt-4">
        Emergency procedures and medical assistance guides
      </p>
    </div>

    <!-- Search Bar (ransack) -->
    <div class="mt-8 max-w-3xl mx-auto">
      <%= search_form_for @q, url: categories_path, html: { class: "relative rounded-md shadow-sm" } do |f| %>
        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
          <svg class="h-5 w-5 text-gray-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z" clip-rule="evenodd" />
          </svg>
        </div>
        <%= f.search_field :category_or_title_or_description_or_topics_title_or_topics_description_cont, 
          class: "focus:ring-red-500 focus:border-red-500 block w-full pl-10 pr-12 py-3 border-gray-300 rounded-md",
          placeholder: "Search categories and topics..." %>
        <div class="absolute inset-y-0 right-0 flex items-center">
          <%= f.submit "Search", class: "px-4 py-2 bg-red-600 text-white rounded-r-md hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 cursor-pointer" %>
        </div>
      <% end %>
    </div>

    <!-- Categories Grid -->
    <div class="mt-12 grid gap-8 md:grid-cols-2 lg:grid-cols-3">
      <% @categories.each do |category| %>
        <div class="flex flex-col rounded-lg shadow-lg overflow-hidden bg-white hover:shadow-xl transition duration-300 ease-in-out">
          <!-- Category Header -->
          <div class="flex-shrink-0 bg-red-600 px-6 py-4">
            <h2 class="text-xl font-bold text-white">
              <%= link_to category.category.titleize, category_path(category), class: "hover:text-red-100" %>
            </h2>
          </div>
          
          <!-- Category Content -->
          <div class="flex-1 p-6 flex flex-col justify-between">
            <div class="flex-1">
              <p class="text-sm text-red-600 font-medium">
                <%= pluralize(category.topics.count, 'topic') %>
              </p>
              <p class="mt-3 text-base text-gray-500">
                <%= category.description %>
              </p>
            </div>
            <div class="mt-6">
              <div class="flex space-x-1">
                <% category.topics.limit(3).each do |topic| %>
                  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
                    <%= link_to topic.title.truncate(15), category_topic_path(category, topic) %>
                  </span>
                <% end %>
                <% if category.topics.count > 3 %>
                  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                    +<%= category.topics.count - 3 %> more
                  </span>
                <% end %>
              </div>
            </div>
          </div>
          
          <!-- View All Button -->
          <div class="bg-gray-50 px-6 py-4">
            <%= link_to "View all #{category.category.titleize} topics", category_path(category), class: "text-sm font-medium text-red-600 hover:text-red-500" %>
          </div>
        </div>
      <% end %>
    </div>

    <!-- Emergency Contacts CTA -->
    <div class="mt-16 bg-red-50 rounded-lg shadow overflow-hidden">
      <div class="px-6 py-12 sm:px-6 sm:py-16 lg:px-8 lg:py-20">
        <div class="max-w-2xl mx-auto text-center">
          <h2 class="text-2xl font-extrabold text-gray-900 sm:text-3xl">
            <span class="block">Need immediate help?</span>
            <span class="block text-red-600">Emergency Contacts</span>
          </h2>
          <p class="mt-4 text-lg leading-6 text-gray-500">
            Quick access to local emergency services and important contacts
          </p>
          <div class="mt-8 flex justify-center">
            <%= link_to "View Emergency Contacts", emergency_contacts_path, class: "inline-flex items-center px-6 py-3 border border-transparent text-base font-medium rounded-md shadow-sm text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500" %>
          </div>
          <div class="mt-6" data-controller="csv-import">
            <h3 class="text-xl font-semibold mb-6">Import Data?</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6" data-controller="csv-import">
          <!-- Category CSV Import -->
          <div class="bg-white rounded-lg shadow p-2">
            <h4 class="text-lg font-semibold text-blue-600 mb-2">Import Categories</h4>
            <%= form_with url: import_categories_path, method: :post, html: { multipart: true, data: { csv_import_target: "form" } } do |f| %>
              <%= f.file_field :file, class: "block w-full mb-2 bg-gray-100 border border-gray-300 rounded px-2 py-2 text-sm cursor-pointer file:cursor-pointer", data: { csv_import_target: "file" } %>
              <%= f.password_field :password, placeholder: "Confirm password", class: "block w-full mb-2 border border-gray-300 rounded px-2 py-1", data: { csv_import_target: "password" } %>
              <%= f.submit "Import Category CSV", class: "w-full px-4 py-1 bg-blue-600 text-white rounded hover:bg-blue-700", data: { action: "click->csv-import#submit" } %>
            <% end %>
          </div>
          <!-- Topic CSV Import -->
          <div class="bg-white rounded-lg shadow p-2">
            <h4 class="text-lg font-semibold text-blue-600 mb-2">Import Topics</h4>
            <%= form_with url: import_category_topics_path(@categories), method: :post, html: { multipart: true, data: { csv_import_target: "form" } } do |f| %>
              <%= f.file_field :file, class: "block w-full mb-2 bg-gray-100 border border-gray-300 rounded px-2 py-2 text-sm cursor-pointer file:cursor-pointer", data: { csv_import_target: "file" } %>
              <%= f.password_field :password, placeholder: "Confirm password", class: "block w-full mb-2 border border-gray-300 rounded px-2 py-1", data: { csv_import_target: "password" } %>
              <%= f.submit "Import Topic CSV", class: "w-full px-4 py-1 bg-blue-600 text-white rounded hover:bg-green-700", data: { action: "click->csv-import#submit" } %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>