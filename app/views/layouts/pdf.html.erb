<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= content_for?(:title) ? yield(:title) : "Emergency Checklist" %></title>
    <style>
      body { font-family: Arial, sans-serif; margin: 20px; }
      h1 { color: darkred; text-align: center; }
    </style>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
