<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Emergency Guide App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
  </head>

  <body class="bg-gray-50 text-gray-800 font-sans">
    <%= render 'shared/navbar' %>

    <main class="pt-18 px-4">
      <%# <div class="max-w-5xl mx-auto text-center">
        <h1 class="text-4xl font-bold text-gray-800 mb-4">Welcome to the Emergency Guide App</h1>
        <p class="text-lg text-gray-600">
          Your quick-access tool for survival info, emergency contacts, and first aid help.
        </p>
      </div> %>

      <%= yield %>
    </main>
    
    <div class="fixed bottom-0 left-0 w-full">
      <% flash.each do |type, message| %>
        <% next if message.blank? %>
        <div class="<%= flash_class(type) %> py-2 px-4 text-center italic">
          <%= message %>
        </div>
      <% end %>
    </div>
    
  </body>
</html>
