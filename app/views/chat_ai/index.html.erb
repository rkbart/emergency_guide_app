<div class="min-h-screen bg-gray-50 flex flex-col items-center justify-start p-4">
  <div class="bg-white shadow-xl rounded-2xl p-6 w-full max-w-2xl border border-gray-200">
    
    <div class="mb-6 text-gray-700 text-center">
      <div class="flex items-center justify-center space-x-2 mb-3">
        <svg class="w-6 h-6 text-red-600" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2h-1V9z" clip-rule="evenodd" />
        </svg>
        <h2 class="text-xl font-bold text-red-600">Emergency Response AI</h2>
      </div>
      <p class="text-gray-800">
        Your virtual emergency assistant. Get expert guidance for first aid, survival techniques, and crisis management.
      </p>
    </div>
    
    <h1 class="text-2xl font-bold text-gray-800 mb-4 text-center border-b border-gray-100 pb-2">
      Ask a Question
    </h1>

    <%= form_with url: ask_chat_ai_index_path, method: :post, class: "space-y-4", data: { turbo: false } do |form| %>
      <div>
        <%= form.label :question, "Describe your emergency situation:", class: "block text-sm font-medium font-bold text-gray-700 mb-1" %>
        <%= form.text_area :question, rows: 4, 
              class: "w-full p-3 border h-18 border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-1 focus:ring-red-500 focus:border-red-500 resize-none",
              placeholder: "e.g. Someone is choking, what should I do? How to treat a deep cut?" %>
      </div>

      <div class="text-center">
        <%= form.submit "Get Help", 
              class: "w-full md:w-auto px-6 py-2 border border-transparent rounded-lg shadow-sm text-sm font-medium text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 transition cursor-pointer" %>
      </div>
    <% end %>
    
    <div class="mt-3 flex justify-center">
      <div class="bg-red-50/50 rounded-lg px-3 py-2 inline-flex max-w-lg">
        <p class="text-sm italic text-gray-700 text-center">
          <strong class="font-medium not-italic text-red-600">Important: </strong>
          This AI provides general guidance only. Always call emergency services first in life-threatening situations.
        </p>
      </div>
    </div>

    <div id="answer-container">
      <% if local_assigns[:answer] || @answer %>
        <%= render partial: 'answer', locals: { answer: (local_assigns[:answer] || @answer) } %>
      <% end %>
    </div>

  </div>
</div>

