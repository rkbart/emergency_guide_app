<div class="mt-6 p-4 bg-red-50 border border-red-100 rounded-lg">
  <h2 class="text-lg font-semibold text-red-800 mb-2 flex items-center">
    <svg class="w-5 h-5 mr-1 text-orange-700" fill="currentColor" viewBox="0 0 20 20">
      <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2h-1V9z" clip-rule="evenodd" />
    </svg>
    Answer:
  </h2>
  <div class="whitespace-pre-line text-gray-700">
    <%= answer %>
  </div>
  
  <% if chat.present? && user_signed_in? %>
    <div class="mt-4 flex justify-end">
      <% if current_user.favorites.exists?(favoritable: chat) %>
        <%= button_to "Remove from Favorites", 
              favorite_path(current_user.favorites.find_by(favoritable: chat)), 
              method: :delete, 
              class: "text-sm text-red-600 hover:text-red-800" %>
      <% else %>
        <%= button_to "Add to Favorites", 
              favorites_path(favoritable_id: chat.id, favoritable_type: 'Chat'), 
              method: :post, 
              class: "px-4 py-2 bg-orange-700 text-white rounded hover:bg-orange-800 text-sm cursor-pointer" %>
      <% end %>
    </div>
  <% end %>
</div>